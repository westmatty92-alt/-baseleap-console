# CLAUDE.md — Baseleap Console

This file tells Claude Code how to work in this repo. Read it fully before any task.
**Source-of-truth docs live IN THIS REPO:** read `MASTER_BUILD_GUIDE.md` for the build sequence and `REFERENCES.md` for reference material before starting a module. (The canonical spec also lives in the Notion page "Baseleap Console — Build Spec & Data Contracts", but the in-repo guides are what you read directly.) When a data contract changes, update the in-repo guide in the same session.

## 🛑 THE ONE RULE
Before writing any code:
1. Read `MASTER_BUILD_GUIDE.md` and build the NEXT unbuilt item in dependency order — not whatever seems next.
2. Write a Definition of Done for that item.
3. Get Matthew's approval in plan mode.
4. Build to that spec.

**Tripwire:** if you catch yourself mid-build with undefined requirements, STOP and write the spec first.

## 🗺️ PLAN VISUALLY
For any architecture or structural decision (a data model, a new module's data flow, a pipeline, how components connect), produce a visual map as part of the plan — SVG + PNG, not just prose. Show components as nodes, the connections between them, and built-vs-planned (solid = exists, dashed = planned). Keep the source files in `docs/planning/` and embed the PNG in the relevant Notion spec. Anything with structure or multiple connected pieces earns a map.

## Lessons from session review — read every session
- When a test result gates a commit approval, write the pass/fail lines explicitly in your response text — don't rely solely on a tool-result block, which can get truncated/collapsed when relayed for review.
- When a third-party API's scope/permission name doesn't resolve after one attempt, check the provider's actual permission UI directly — don't infer the scope name from the endpoint's URL structure.
- When you notice a gap between "the API/proxy technically supports X" and "nothing in our data model actually declares X yet," say so explicitly rather than building past it or assuming it away. This matters most for the catalog/manifest system, where declaring more than the data model supports would produce silently wrong behavior later.
- Test your own new code's rejection paths (unknown actions, malformed input, invalid payloads) as reflexively as its happy path — before considering it verified.
- Comments on security-relevant lines should state the property being protected ("X can never happen because Y"), not just describe the mechanism.

Full reasoning and evidence for these lives in the Notion Baseleap Orchestrator doctrine (§3.3), added July 6 2026.

---

## WHAT THIS IS
The Baseleap Console — an internal agency tool Baseleap uses to win and onboard clients. A single-file HTML app (`index.html`) + a Vercel serverless AI proxy (`api/ai.js`), backed by Supabase (RLS on every table).
- Internal only. Not resold, not embedded in client GHL sub-accounts. Separate from Pulse (a client-facing product) and from the future master CRM.
- Houses a client-acquisition suite behind a business-switcher dropdown. One client = one row in the `clients` table.
- Five modules: Client Research, AI Audit Assistant, Automation Agent, Gap Report Builder, Proposal Generator. (Client Research is planned but not yet on the near-term roadmap below.)

## CURRENT BUILD TARGET
- Built: Console shell (auth + business-switcher + state-reset), AI proxy, Module 1 Audit Assistant, Module 2 Automation Agent Phase A (feasibility gate).
- Next: build-planner (Phase C) + Setup Agent → Proposal Generator + scope gate. (GHL capabilities reference + boundary map done → .claude/skills/ghl-automation + ghl-setup.)

---

## ARCHITECTURE RULES (non-negotiable)
- Single `index.html` — no separate module files. Modules are tabs/sections inside it.
- All Claude calls go through the `ai()` wrapper → `/api/ai`. NEVER a raw fetch to api.anthropic.com (CORS + key leak).
- Supabase RLS on every table. Every INSERT stamps `operator_id: S.user.id`. An UPDATE to an existing row does NOT re-stamp operator_id (RLS already covers it).
- New per-client state goes in `resetClientState()` (via `freshAudit()` etc.) so nothing bleeds between clients on switch.
- Modal backgrounds: hardcoded hex, never CSS vars (vars inherit transparency).
- Keep functions short. No monolithic mega-functions (the openAdView SyntaxError trap).
- Defensive JSON parse on all AI output — strip ```json fences, try/catch, and validate the response SHAPE, not just that it's valid JSON.
- Schema changes run MANUALLY in the Supabase SQL editor (GitHub is not wired to Supabase). Migrations live in `/migrations`. Use `ADD COLUMN IF NOT EXISTS`.
- Verify a column exists in the live DB before writing to it — never trust the docs alone; check the actual schema.
- Every Supabase write the UI depends on must `.select()` and verify the returned row. A bare `.update()` reports false success on a 0-row write — always chain `.select().single()` and re-render from the returned row.

## GIT / DEPLOY DISCIPLINE
- Ask before `git push`. Do not push without explicit approval.
- Verify a commit reached `origin/main` before trusting a Vercel redeploy. A push that never happened makes Vercel faithfully redeploy old code while appearing successful.
- Env var `ANTHROPIC_API_KEY` must be set on Vercel Production (not Preview-only); redeploy after changing env vars.
- Repo has a leading hyphen: `-baseleap-console`. Use `./` in shell paths.

## BRAND TOKENS (use these for all UI — no other colors)
- Midnight `#0D1F2D` (primary dark) · Volt `#00D4A0` (accent) · Canvas `#F4F5F0` (light bg) · Depth `#1A1A2E` (text/dark) · Run `#00A878` (secondary green)
- Headline font: Garet ExtraBold.
- Tokens are defined in `:root` in `index.html` — use those, don't hardcode hex outside it (modal backgrounds are the one documented exception above).

---

## DATA CONTRACTS (see MASTER_BUILD_GUIDE.md / REFERENCES.md for full detail)
- `clients` — one row per client/prospect. `current_stack` (JSONB) written by the Audit Assistant Stack tab (UPDATE), read by the Automation Agent. Archive with status; don't hard-delete.
- `gaps` — one row per atomic gap. Lifecycle: Audit Assistant writes `pending` (title/problem/cost/severity/category) → Automation Agent writes `feasible/mechanism/estimated_hours/approach` and flips to `validated`. Never let the Audit Assistant set `validated`. `matched_engine` is pattern metadata, NOT a retrieve guarantee: the sweep's DEPTH GUARD (`isDeepEngine` — live row must have `spec.manifest` + real workflow content) decides retrieve vs seeded formulate. A match requires `match_evidence` in the assessment JSON (enforced at parse, folded into rationale).
- `gaps.client_copy` / `gaps.client_copy_fingerprint` (JSONB / TEXT, migration 013) — the CLIENT-FACING synthesized Gap Report copy. The raw `problem`/`cost` columns stay the internal audit transcript (first-person interview shorthand) and are NEVER shown to a client verbatim — they appear ONLY in the ungenerated state, explicitly marked internal-only. `client_copy` = `{ problem, cost, solution, savings, route:'cataloged'|'formulated', phase:'conclusion'|'complete', generated_at, solution_at }` — the four professional third-person sections a prospect reads. TWO-PHASE, cached with EVENT-DRIVEN regeneration: Phase 1 (Conclusion + Cost, from the raw transcript) fires on validate (`generateGapCopy`, fire-and-forget, never faults the validate); Phase 2 (Solution + Savings) once grounding exists — a matched engine's catalog `spec` immediately, a formulate gap's build step at build-save (`kickBuildGapCopy` → `advanceGapCopyPhase2`). Phase 2 ALWAYS receives Phase 1's text (continuity). Solution grounding is THREE-TIER (`gapSolutionSource`): deep catalog `spec` → engine stub + gap mechanism → the gap's build step — catalog `spec`, NEVER a client's deployed snapshot (Gap Report is client-level). Anti-paraphrase doctrine (`GAPCOPY_DOCTRINE`) is the core safeguard: synthesize the STRUCTURAL failure not the anecdote, third person, preserve source numbers but fabricate none (internal build-hours are cost-to-deliver, NEVER presented as client savings), client-safe (no vendor/tool terms). Validators (`validateConclusionCopy`/`validateSolutionCopy`) enforce shape + first-person + length; genuine synthesis is confirmed by READING real output, not by parsing. `client_copy_fingerprint` = djb2 hash of raw `problem+cost+matched_engine+mechanism+approach` → a mismatch means the source changed = copy stale → regenerate. Concurrency: a per-gap `copyBusy` lock shared by `generateGapCopy` + `advanceGapCopyPhase2` (finally-guaranteed clear) + a `copyAll` bulk guard. Render states: ungenerated / interim (formulate awaiting build) / incomplete (Phase 2 failed) / complete.
- `Gap Report client-facing Preview` (`.cr-*` view, `renderClientReport()`, index.html) — a separate branded/print presentational layer over the operator Gap Report tab, reached via a **Preview** button. Reads the SAME `client_copy` data, no new AI/DB calls. **Non-negotiable gate: only gaps with `client_copy.phase === 'complete'` may appear** — an incomplete/interim gap is excluded outright, never shown with placeholder text, so a client never sees anything but finished, synthesized copy. Full dark theme (Midnight throughout), real logo asset (`assets/baseleap-logo.png`), adaptive `.cr-close` wrapper keeps the closing callout+footer together without a forced page break. PRINT PAGE GEOMETRY (corrected Aug 1 2026 — the previous contract read `@page{margin:0}` for edge-to-edge print, which is what made every body page after the first start flush at the sheet edge): `@page{margin:52px 0;background:#0D1F2D}` reserves per-page breathing room, `@page:first{margin:0}` exempts the cover so its photo band keeps its full bleed (and `.cr-cover`'s `min-height:96vh` still resolves against the whole sheet), and `.cr-body{padding-top:0}` inside `@media print` cancels the screen-only `padding:52px 60px 0` so the FIRST body page doesn't double to 104px. THE TRAP — the `@page` margin band lies OUTSIDE the normal box model: no element background reaches it, not `body`'s and not `html`'s, so with a nonzero margin the band prints WHITE and the dark report gets a white frame on every body page. ONLY `@page`'s own `background` property fills it, and it must be a LITERAL hex (`#0D1F2D`) — `@page` has no element context to resolve `var(--midnight)` against dependably. This is why the old contract said `margin:0`: zeroing the margin was avoiding the white frame rather than solving it. `.cr-body`'s padding is a ONE-TIME offset at the start of the content flow and can never space page 2 onward — per-page space comes only from `@page`. Verified by print-to-PDF raster measurement: top ink 52–56px on every body page (spread 55px → 4px, the residual being glyph leading), bottom never below the reserved 52px, zero non-Midnight perimeter pixels on pages 1–8; page 9's Volt pixels are the full-width footer band bleeding to the side edges, pre-existing and unchanged (145px on both edges, displaced down exactly 52px). Merged to `main` in PR #3 (`c40f05f`).
- `Setup Agent chat panel` (`.setup-split`/`.setup-chat`, `renderSetup()`/`renderSetupChatPanel()`, index.html — layout shell July 30 2026, LIVE EXECUTION Aug 3 2026; map: `docs/planning/setup_chat_nav_derived_state.svg`) — a third column beside the Setup Agent content, opened by "Ask Setup Agent". The composer is NO LONGER INERT (this bullet previously read "NO AI, no message model, no persistence, no DB work yet; the composer is inert"): the chat carries a message model, a mandatory confirmation gate and REAL GHL writes through the same `runSetupItems` the build plan uses — namespaced previews + one client-wide execution lock, chat runs stamped `build_plan_id: null` / `checklist: []`. Proven live in production (calendar "deliveries", `setup_runs` `60f985cd`). THE AI LAYER IS NOW BUILT (Aug 4 2026 — this bullet previously read "Only the AI layer is still unbuilt: `proposeChatManifest` is a regex stand-in and is THE seam commit 3 replaces"): `proposeChatManifest` is a real `ai()` call and is the ONLY AI in the chat path. Output is a DISCRIMINATED UNION — `{action:"propose",summary,manifest}` or `{action:"reply",message}` — so refusing is a first-class response, never a degenerate manifest; a `reply` STOPS BEFORE `preflightSetup` (no inventory read, no diff, no confirm), which is what makes a manifest attached to a `reply` by a hedging model unreachable by construction. Scope is EXACTLY the five kinds `runSetupItems` can write; out of scope → state it plainly and stop, never approximate with a near-miss item. The AI is deliberately BLIND to the live inventory (it translates intent; `computeSetupDiff` against a fresh preflight stays the single source of truth for what already exists). VALIDATION CHAIN before anything reaches the pipeline: `JSON.parse` → `validateChatProposal` (shape) → **`validateManifest` UNMODIFIED** (the same validator gating formulate) → `hardenChatManifest`. That last one is chat-only and ADDITIVE ON PURPOSE — folding it into the shared validator would change formulate's behavior — and closes three SILENTLY-WRONG-WRITE holes `validateManifest` genuinely does not cover: field `type` outside `text|number|currency|date` (`setupFieldItem` does `GHL_DATATYPE[type] || "TEXT"`, so `"dropdown"` would become a TEXT field and a capitalized `"Number"` would too — hence case-insensitive accept + canonicalize to lowercase), unknown top-level keys (an invented sixth kind is ignored by `setupManifestUnion` = silently dropped while the summary still claims it), and DUPLICATE PIPELINE STAGE NAMES (`setupPipelineItem` maps stages to `{name,position:i}` and would create both). Reuses `aiJsonWithRetry` unchanged in doctrine (+ additive `history`/`tag` params). PROVEN LIVE Aug 4 2026 against the `215ff94` preview — five cases, including an out-of-scope request correctly producing a refusal with NO diff; case 1 left `setup_runs` `70c9c628` (calendar `g money`, `build_plan_id` NULL). The refusal path leaves NO server-side trace BY DESIGN, so it is corroborable only by operator observation — that is correct behavior, not missing evidence. NOT mechanically detectable, so don't mistake it for covered: a summary that misdescribes its own manifest — the confirmation gate is the control, the operator confirms against the rendered diff, never the summary. Full contract: MASTER_BUILD_GUIDE.md "Setup Agent chat — the AI layer". SIDEBAR OVERRIDE IS TWO INDEPENDENT CLASSES: `#app.nav-collapsed` = the operator's PERSISTED preference (☰ / `toggleNav()` / `localStorage bl_nav_collapsed`), `#app.nav-forced` = the TRANSIENT chat-open override. `setNavForced(on)` toggles ONLY the class and NEVER reads/writes the pref key — that is exactly what lets release restore whatever the operator had (collapsed OR expanded) instead of guessing; `localStorage.setItem` occurs exactly ONCE in the file (in `toggleNav`), which is the structural proof. Selectors are GROUPED in CSS so they can't drift. THE OVERRIDE IS DERIVED, NEVER SET IMPERATIVELY: one line, first statement of `render()` — `setNavForced(!!S.activeClient && S.module === "setup" && !!S.setup.chatOpen)` — so chat-close, module-switch, client-switch and no-client all release it by construction (they already funnel through `render()`). The `!!S.activeClient` term is LOAD-BEARING and position above the early return is NOT sufficient alone: with no client, `S.module` is still `"setup"` and `chatOpen` still true, so dropping it strands the override while the early return replaces `#content` with the empty-client message — the chat's ✕ is gone and ☰ can't recover the sidebar (`nav-forced` hides it independently of `nav-collapsed`) = soft-lock until reload. `chatOpen` lives in `freshSetup()` so `resetClientState()` closes the chat on client switch. The chat column is a SIBLING of `#setup-body`, NEVER a child — `renderSetupBody()` rewrites `#setup-body` alone after every setup action, so nesting would wipe composer text + scroll. `.content`'s own CSS is UNTOUCHED (shared scroll container for all four modules) and the topbar still spans full width. `--topbar-h:67px` is MEASURED (14+14 padding + 1 border + 38 tallest child) and APPLIED to `.topbar` as an explicit height, not just consumed by `calc()`, so the value can't drift from the element — the topbar is therefore PINNED: a taller control added to it must bump `--topbar-h` in the same change or clip.
- `audit_sessions` — one row per audit (transcript/summary/status); gaps FK via `audit_session_id`.
- `build_plans` — parent record per engagement (post-agreement only). Build steps in dependency order, status `queued → building → testing → done`. Supports cross-agent step dependencies.
- `build_steps.manifest` / `build_steps.workflow` (JSONB, migration 008) — Build-Plan Depth. `manifest` = the field/tag manifest (`requires_tags`/`creates_tags`/`requires_fields`, plus three OPTIONAL setup-item kinds — see the SETUP ITEM KINDS bullet below) on the engine's artifact step and on code-injected "create tags & fields" setup steps; `{"tbd":true}` exists only on HISTORICAL rows — the depth guard means a thin engine now formulates (with its own manifest) instead of retrieving empty. `workflow` = the structured node workflow (automations array; node types `trigger|guard|wait|action|update|condition|webhook|handoff|end` — webhook stays distinct from action, it marks the external/n8n boundary). Rendered only on step expand; PM layer (status/checklist/hours/notes) is untouched. GUARD RENDER RULE (locked July 8 2026): a guard IS a GHL if/else, so the node graph ALWAYS draws it as a fork — condition met → End pill, else → the remaining flow (cascading guards nest into successive forks, matching the real GHL canvas). Guard stays a SINGLE node in the data (`{type:"guard", config:"If X → end"}`); the fork is synthesized at render (`renderNodeGraph`) — never make the AI emit branch arrays on guards. MULTI-BRANCH (July 9 2026 — the old "3+-way split not expressible" gap is CLOSED): a guard/condition with 3+ outcomes carries a `branches:[{when, to:[nodes]}]` array — up to 5 condition branches (GHL's If/Else limit); the None/else path is the nodes that FOLLOW the node in the automation (the main flow — don't repeat it in branches). Formulate emits it; validated (`validateNodes`): ≤5 branches, each a non-empty `when` string + a non-empty `to` node array. A single-condition guard stays `config:"If X → end"` (rendered as the 2-branch if-met/if-not-met case). EMPTY-BRANCH AUTO-REPAIR (locked July 14 2026 — do NOT revert to a rejection): a `condition` node with exactly ONE empty branch (`yes` empty XOR `no` empty) is the recurring shape the model emits when it means a guard (one side stops, one continues). `validateNodes` → `normalizeLoneEmptyBranch` deterministically repairs it in place — the empty side becomes an explicit `{type:"end",config:"End"}`, the populated side stays — which renders identically to a guard (met→end / else→continue). Kept as `type:"condition"` (NOT converted to `type:"guard"`) ON PURPOSE: the canvas guard renderer consumes the FOLLOWING siblings as its continue-flow and ignores yes/no, so a literal guard conversion would silently DROP the populated branch. This SUPERSEDES prompt-only prevention (c79e2d4 empty-no + the symmetric empty-yes rule), which never converged across three occurrences. BOTH branches empty is still a real fallback-less If/Else and still THROWS — only the exact one-empty shape is repaired (a `console.warn` leaves an audit trail so an unusual spike in repairs is visible). NODE GRAPH (July 9 2026): nodes render as a TYPE LABEL only (Trigger/Guard/Action/…) on a dotted canvas — filled brand pill for the trigger start + End/Stop exits, flat color-coded cards for the rest, connectors as thin lines with a junction dot; a zoom control (40–200%, `S.buildPlan.zoom` + `pan`) sits bottom-right and the box is a drag-to-pan / wheel-zoom canvas. Clicking a node opens a popup with its details. GUARD/CONDITION RENDER (updated July 9): drawn as an explicit If/Else FORK (`renderFork`) — a bracket of branch columns (each labelled with its `when`; a single-condition guard = met→End / else→continue) plus a None column carrying the main flow. Cascading guards nest (staircase) like the real GHL canvas; pan/zoom navigates it. Guard stays a single data node; the fork is synthesized at render. Per-node OPTIONAL `deploy:{input,assets,guardrail}` bucket (inside `workflow` JSONB — no migration): `input` = what the operator enters for this client, `assets` = copy/links to attach (message nodes only), `guardrail` = what must not change here. The popup shows config + these three as labeled sections; formulate emits them (validated lenient), review_engine carries them by hand. Deployment split: `branding` + `never_touch` moved OUT of the side Deployment card into node popups (assets/guardrail); the card keeps changes_note, parameter_values (concrete per-client values), business_rules, overrides, spec_extras. Detail sidebar order: Manifest → Setup Checklist → Deployment → Notes.
- SETUP ITEM KINDS (Order 15.7, July 29 2026) — three ADDITIVE OPTIONAL `build_steps.manifest` fields extend the manifest past tags/fields, and the Setup Agent's mechanical plumbing for all five kinds. Declared shapes: `requires_calendars:[{name}]`, `requires_custom_values:[{name,value}]`, `requires_pipelines:[{name,stages:[stage-name-strings]}]` (stages non-empty). OPTIONAL means absent — undefined OR null — reads as `[]`, so every manifest authored before these existed stays valid; present-but-malformed still throws (`validateManifest` → `validateManifestSetupItems`). Shapes are EXACTLY the live-verified write matrix in `.claude/skills/ghl-setup` (API appendix) and nothing more: a key we can't create through a probed endpoint has no business being declared. Stage `position` is REQUIRED by the API (missing → 422) but is DERIVED from array index at execution (`setupPipelineItem`), never authored in the manifest — declaration order IS stage order, which is what the preview shows for confirmation. MECHANICAL PATH: `setupManifestUnion` (unions + dedupes by CASE-INSENSITIVE name, first-wins — two spellings must never both reach the create loop; a `[…]`-prefixed name, or a `[…]` custom-value VALUE, routes to `paramItems` for hand-naming and is NEVER created literally, so a placeholder never gets written into a client's account) → `computeSetupDiff` (`*ToCreate`/`*Existing` via `missingByName` against the `preflightSetup` inventory, matched by NAME ONLY: an existing pipeline whose stages differ, or a custom value holding a different value, counts as EXISTING and is left untouched — setup creates what's missing and never mutates a live client asset, so a stage/value mismatch surfaces only in the human checklist) → `renderSetupDiff` ("N to create · N already exist" per kind, lines shown only when the union declares that kind; `setupCreateCount` is the single source of truth for the confirm button's count so the promise can't drift from what the loop writes) → `confirmRunSetup` (items ordered by the skill's foundational sequence tags→fields→custom values→calendars→pipelines; EXPLICIT per-kind dispatch whose terminal `else` THROWS — never a bare `else`, so an unrecognized kind can't fall through to a mismatched handler and write the wrong asset; the throw lands in the existing catch → logged → FAIL-STOP) → `setupCalendarItem`/`setupCustomValueItem`/`setupPipelineItem`. Those three carry NO try/catch ON PURPOSE: the appendix documents duplicate-create recovery ONLY for tags (400 "already exist") and fields (400 + `meta.existingId`), and there is no verified duplicate-error shape for the other three — so none is invented. The preflight-inventory lookup is what prevents a duplicate create being attempted at all, and any genuine API error propagates untouched to the fail-stop. `mergeGhlMap` buckets returned ids under `calendars`/`custom_values`/`pipelines` (additive JSONB on `clients.ghl_map`, no migration, same deep-merge-never-overwrite property). CALENDAR SHAPES VERIFIED (Aug 3 2026 — this bullet previously read UNVERIFIED — PROBE BEFORE TRUSTING, because the appendix spelled out list-row and create-envelope shapes for tags/fields/custom values/pipelines but ELIDED both for calendars, leaving `missingByName` reading `x.name` and `setupCalendarItem` reading `r.calendar.id` INFERRED). Both probed live against "123 business": list returned `{calendars:[…], traceId}` with every row carrying a non-empty string `name` + `id`; `create_calendar` returned `{calendar:{id,…}, traceId}` with the id at `r.calendar.id` — the alternatives `r.id` and `r.data.id` were probed and confirmed ABSENT — and a re-list showed the new calendar with the same id. **BOTH ASSUMPTIONS WERE CORRECT; no code changed.** Probe artifacts persist in the sub-account (the proxy exposes no calendar delete): `Console Probe Calendar 20260803`. NOT YET WIRED (deliberately a separate later pass): formulate's system prompt and `injectManifestSetupSteps` do NOT emit these three fields, so nothing declares them yet and their preview lines stay hidden until something does.
- `build_steps.deployment` (JSONB, migration 009) — the full auto-generated deployment story on the engine's artifact step: `{ route, seeded_from, needs_review, changes_note, parameter_values, tiers, overrides, spec_extras }` (`seeded_from` = the thin catalog stub a formulation was seeded with, else null). Renders as the READ-ONLY collapsed "Deployment guide" section (⚠ REVIEW inline on the toggle for parameterize/formulate). `notes` holds ONLY operator-typed text on new plans; historical rows keep their old baked-in dump (never rewritten) with `deployment = NULL`.
- `build_steps.track` / `build_steps.phase` (migration 011) — ADDITIVE PREP, no UI yet. `track` (TEXT, default `'automation'`, CHECK `automation|agent|app`) = which product line the step belongs to, for a future unified multi-track build plan (CRM + Agent + App in one view); all existing rows backfilled to `'automation'` so current CRM-only plans are unchanged. `phase` (TEXT, nullable, no CHECK) = free-text label for that future phased view (vocabulary deliberately undefined; ordering still lives in `position`). NOTE: `track` != the existing `agent` column — `agent` (`setup|automation`) = which sub-agent builds the step; the shared string `'automation'` is intentional and semantically distinct. Names kept as-is by decision. Nothing reads/writes these two columns yet.
- `PM Tracker` (`renderTracker()`/`renderTrackerBody()`, `.trk-*`, Tracker subtab, index.html — Order 15, Aug 4 2026) — a LIFECYCLE LENS over the SAME `S.buildPlan.steps`, NOT new machinery. THE LIFECYCLE ALREADY EXISTED: `build_steps.status` has carried the live CHECK `queued|building|testing|done` since migration 005, `STATUS_STAGE` maps it to 4 stages, `setStepStatus()` writes it and stamps `completed_at`, and `renderEngineRow` already drew bar + blocked-by + actionable. Order 15 added NONE of that — it adds the arrangement plus three facts the plan view computed then discarded: the actionable QUEUE, blocker IMPACT (`blockerImpact` → "unblocks N"), and `completed_at` (which had ZERO read sites before). `trackerBuckets` is EXHAUSTIVE + DISJOINT BY CONSTRUCTION — `done` and `queued` handled explicitly, everything else falls to a CATCH-ALL in-flight branch (so a status added to the CHECK later surfaces instead of vanishing) — so the four buckets always re-sum to `steps.length` and can't contradict the header. Buckets are `position`-sorted, making the top of Ready the real "do next". `stepBlockers(s, opts)` / `isActionable(s)` are THE single definitions, extracted from two inline copies so three surfaces can't drift; `opts.skipSameGap` is a DISPLAY scope, NOT a truth change (a nested gate IS genuinely blocked by its own engine — the Tracker calls it WITHOUT the option and buckets that gate as Blocked). A DONE step NEVER renders blocked-by: real plans contain done steps whose deps are still queued (work completed out of dependency order), so the guard is `s.status !== "done"` ON THE LABEL ONLY — the computation was never weakened to fix a display problem (found on real data, not fixtures; `renderEngineRow` still has the same cosmetic issue and was deliberately left out of scope). NO new write path and NO schema change: rows drive the existing `setStepStatus()`, `build_steps` has exactly 3 update sites (status/checklist/notes) with only one writing status, and the plan-level rollup is DERIVED-only — `build_plans.status` is free text with NO CHECK and is not written here. DEFERRED, NOT DROPPED: the `done`-click 3-audience SOP generation + deployed-system ledger write, because neither had a substrate. **The ledger half is now BUILT — see the `deployed_systems` bullet below (Order 16, Aug 5 2026) — but deliberately NOT wired to the `done` click**; the SOP half is still substrate-less (the three audiences are undefined anywhere in the repo). Full contract: MASTER_BUILD_GUIDE.md "PM Tracker — lifecycle lens".
- `deployed_systems` (migration 014, Order 16 v1, Aug 5 2026 — `isDeployableEngine`/`markDeployed`/`renderDeployedCard`, `.dep-*`, index.html; map: `docs/planning/order_16_deployed_systems_ledger.svg`, plan: `docs/planning/order-16-deployed-systems-ledger.md`) — the record that an ENGINE IS LIVE IN A CLIENT'S ACCOUNT. **IT IS AN OPERATOR ASSERTION, NOT A DERIVATION, AND THAT IS THE WHOLE POINT:** the Setup Agent can mechanically create exactly the five SETUP ITEM KINDS and **workflows are not among them** — engines are hand-built in GHL, so nothing in the data model can know this. `clients.ghl_map` proves an ASSET exists but is flat and cannot attribute it to an engine or a build; `setup_runs` proves a RUN happened but only over those five kinds. Grain = ONE DEPLOYED ENGINE PER CLIENT (the gap ≈ engine ≈ sellable-unit grain used app-wide). SNAPSHOT + POINTER: `title`/`engine_key`/`mode` are COPIED at write time and the FKs `build_plan_id`/`build_step_id` are **ON DELETE SET NULL, never CASCADE** — deleting a plan cascades its `build_steps` away, so without the snapshot a plan delete would erase the record that its engines are live. ELIGIBILITY IS `isDeployableEngine` (`agent==="automation"` + a non-empty `workflow` array) — **NOT `categorizeSteps().engines`**, which deliberately retains UNMATCHED test gates because that is the slot they RENDER in; the workflow test is the same one `partitionTestGates` uses to find an engine parent, so the two can never disagree about what an engine is. On the real 17-step plan `de43a1b1` both happen to return 4, which is exactly why that agreement proves nothing — the divergence needs an unmatched gate and is asserted with a synthetic one. DELIBERATELY NOT WIRED TO THE `done` CLICK: marking a step done is a PM act; asserting a system is live in a client's account is a different claim with external consequences, so it is its own confirm-gated action (state-driven `S.buildPlan.dep`, never a native `confirm()`). `markDeployed` re-checks BOTH `status==="done"` and `isDeployableEngine` even though no other row renders the control — the function rejects a bad call rather than relying on the render to prevent it. Append-only EXCEPT `removeDeployedSystem` (confirm-gated, `.select()`-verified): a mis-assertion must be correctable. Partial `UNIQUE(build_step_id) WHERE NOT NULL` makes a double-mark a plain message, not a crash — partial because orphaned snapshots (NULL step) must be able to coexist. Load is CLIENT-scoped (`loadDeployedSystems`, degrades to `[]` on error), the Tracker card is PLAN-scoped; the cross-plan "what's live" map anticipated at index.html:4122 is a SEPARATE increment. **v1 NEVER writes `engine_catalog`** — a `formulate` entry gets a READ-ONLY "graduation-eligible" label only, preserving "the Agent proposes; the operator confirms what graduates. Never auto-write — the catalog is also the price sheet." DELETE GUARD: the open-folder delete now counts `deployed_systems` ALONGSIDE `setup_runs` and `deleteBlockReason` names the ACTUAL condition(s), so an operator is never told "there are setup runs" when the real blocker is a live engine; the ledger ROW survives a hard delete either way (SET NULL) — the guard protects the POINTER. Still out of v1 by decision: 3-audience SOP generation, supersession/re-deploy semantics, automatic graduation. Full contract: MASTER_BUILD_GUIDE.md "Order 16 — deployed-system ledger".
- DEPTH GUARD (retrieve-vs-create gate) — `classifyGapRoute` routes retrieve ONLY for engines passing `isDeepEngine` (live `spec.manifest` + every automation an object with steps/nodes). Thin/half-deep engines route to formulate SEEDED with their stub (`catalogSeedBlock`); `engine_key` stays null in the sweep result. Deepen the catalog row → route flips to retrieve, no code change. Assessment prompt carries per-engine DOES/NOT-FOR scope; stub matches estimate first-build hours.
- `engine_catalog.spec.manifest` — per-engine field/tag manifest, merged via `updateEngineManifest()` (browser console; same merge-not-overwrite pattern as `spec.deployment`). Seeded for the 5 deep engines only. Per-engine setup steps are COMPUTED from the union of manifests at plan generation (`injectManifestSetupSteps`) — never hand-listed by the AI.
- `engine_catalog` **test-matrix authoring standard** — every catalog automation's `spec.automations[].tests` MUST be an array of `{name, how}` objects: `name` = a short scenario label; `how` = the click-level what-to-verify (setup + action + expected observable result, at the same depth bar as `deploy.tasks[].how`). NEVER a bare string, and never a single joined string. `testMatrixItems` renders each as an expandable test-gate row; a bare/joined string collapses the written content into one unreadable line (the `lead_response`/`payments_stripe` bug class). This is a **hand-authoring rule** — catalog engines are seeded directly and are NOT run through `validateAutomationTests` (that validator gates AI-formulated output only), so nothing enforces this at runtime; honor it when writing or deepening a catalog engine.

## THE GAP LIFECYCLE (core contract)
A gap is the atomic unit that gets built, feasibility-checked, and priced (~1:1 to a sellable GHL automation). Audit Assistant identifies → Automation Agent qualifies → Gap Report reads only `validated`. Selection state (`proposed|accepted|declined`) drives BOTH the proposal and the build plan — one selection, two projections, always in sync.

## SCHEMA
TWELVE tables, RLS on all (this line read "Seven" until Aug 5 2026 — it had not been updated since the base schema; verify the count against the live DB, not this file): `clients`, `audit_sessions`, `gaps`, `build_plans`, `build_steps`, `build_plan_gaps`, `engine_catalog`, `setup_runs`, `deployed_systems`, `gap_reports`, `proposals`, `client_files`. Schema changes are incremental — see `/migrations` (`ADD COLUMN IF NOT EXISTS`), run manually in the Supabase SQL editor.

## REASONING SKILLS
Each agent reads its skills in-context from `.claude/skills/<name>/SKILL.md` (curated/lean, not a dump). Build a skill alongside its agent, not up-front.

---

## WORKFLOW
1. Read this file, `REFERENCES.md`, and `MASTER_BUILD_GUIDE.md` (build sequence) + the relevant module section before starting. THE ONE RULE applies: the build target is the NEXT unbuilt item in dependency order — not whatever seems next.
2. Read the Notion track plan and the highest-priority incomplete item.
3. Explore the existing `index.html` for the pattern before adding a module — this repo has strong patterns; follow them.
4. Describe your approach with a written Definition of Done and wait for approval before implementing (plan mode). No approval without the Definition of Done.
5. Build → test → verify Supabase writes returned rows → ask before pushing.
6. If a data contract changed, update the in-repo guide in the same session.
7. STANDING CLOSING STEP (every Definition of Done): update the Console Operator SOP
   (Notion page "🖥️ Console Operator SOP" under the Console Build Spec) with the
   feature's operator-facing behavior — alongside bug-ledger and skill updates.
